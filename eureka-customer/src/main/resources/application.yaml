server:
  port: 8082
eureka:
  instance:
    prefer-ip-address: true
#    ip-address: 10.211.169.235
    ip-address: 10.211.96.237
    instance-id: ${spring.application.name}-${eureka.instance.ip-address}:${server.port}
  client:
    service-url:
      defaultZone: http://admin:admin@eurekaServer7001.com:7001/eureka/,http://admin:admin@eurekaServer7002.com:7002/eureka/
spring:
  application:
    name: customer
logging:
  level:
    com:
      example:
        eurekacustomer:
          api:
            UserRemoteClient: DEBUG
            PaymentRemoteClient: DEBUG
ribbon:
  ReadTimeout: 5000
  ConnectionTimeout: 5000